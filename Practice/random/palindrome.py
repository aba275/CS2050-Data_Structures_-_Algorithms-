class palindrome:
    def palChecker(s):
        if len(s) <= 1
            return true
        else:
            if s[0] == s[-1]:
                return palChecker(s[1:-1])
            else
            return false
